# Document Scanner

A computer vision project built for practice.

Takes a photo of a document lying at an angle and automatically detects its edges and corners using Canny edge detection and Harris corner detection, then applies a perspective warp to produce a flat top-down scan.

## Run
pip install opencv-python numpy Pillow

python document_scanner.py
